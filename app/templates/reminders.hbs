<h3>Reminder List</h3>

<ul class="reminder-list">
  {{#each model as |reminder| }}
    <li class="spec-reminder-item">
      {{#link-to "reminders.reminder" reminder}}{{reminder.title}}{{/link-to}}
    </li>
  {{/each}}
</ul>

{{outlet}}
